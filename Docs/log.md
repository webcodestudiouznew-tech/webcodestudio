# log.md

## 2026-03-22
- Добавлена секция `Что получает бизнес` как отдельный компонент с ровной сеткой `3x2`, CTA-блоком снизу и якорем `#business-benefits`.
- Для секции `Что получает бизнес` добавлены локализации `RU / UZ / EN`, синхронизированные с каноническим PRD без новых продуктовых смыслов вне документа.
- Мобильная версия CTA в секции `Что получает бизнес` отцентрована, а заголовок CTA сокращён и зафиксирован в одну строку во всех трёх локалях.
- Секция подключена в [locale]-страницу после `Почему WebCode`.
- Добавлена секция `Что входит в решение` как отдельный компонент с якорем `#solution-includes`, двухколоночным основным блоком, отдельным блоком `Что не входит` и мягким CTA.
- Для секции `Что входит в решение` добавлены и уточнены локализации `RU / UZ / EN`, включая формулировку о том, что стоимость домена и хостинга включена в тариф.
- В секции `Что входит в решение` выполнен проход по mobile-адаптации и выравниванию: центрирование заголовка и описания блока `Что не входит`, выравнивание мини-карточек и корректировка плотности CTA.
- Добавлена секция `Кейсы` с двумя реальными проектами (`Sellium`, `PEAKS`), подключёнными в страницу после `Что входит в решение`.
- Секция `Кейсы` пересобрана в более компактный формат: реальные скриншоты из `public`, accordion-подача деталей, встроенный CTA в каждом кейсе и без отдельного общего CTA-блока снизу.
- Для секции `Кейсы` добавлены и уточнены локализации `RU / UZ / EN`, включая списки `Что сделали`, `Что получил бизнес`, корректные названия кнопок и сокращённый заголовок секции.
- В секции `Кейсы` выполнен проход по mobile-адаптации и визуальной плотности: переработаны пропорции блока со скриншотом, выравнивание правой колонки, горизонтальное растяжение CTA-кнопок и сглаженная анимация аккордеонов.
- Прогнаны проверки `npm run lint` и `npm run build`, обе завершились успешно.

## 2026-03-21
- Секция `Почему WebCode` доведена до рабочего состояния: карточки переработаны в bento-подобную desktop-сетку, синхронизированы тексты `RU / UZ / EN`, добавлен и стилистически доработан CTA-блок.
- Проведён большой проход по mobile-адаптации `Hero`, `Audience` и `Why WebCode`: исправлены заголовки, spacing, card rhythm, CTA-композиция и поведение на узких экранах около `360px`.
- В `Hero` обновлён основной оффер на `Готовый сайт для бизнеса с CRM системой` и синхронизированы переводы для `RU / UZ / EN`.
- Прогнаны финальные проверки `npm run lint` и `npm run build`, обе завершились успешно.
- Добавлена секция `Почему WebCode` как отдельный компонент с якорем `#why-webcode`, сеткой `2x3` и контентом, синхронизированным с каноническим PRD.
- Для секции `Почему WebCode` добавлены локализации `RU / UZ / EN` и синхронизирован визуальный язык карточек с остальными секциями лендинга.
- Из секций лендинга удалена scroll-reveal анимация: убраны view-timeline стили и привязанные reveal-классы в реализации блоков.
- В секцию `Почему WebCode` добавлен обязательный inline CTA `Обсудить ваш проект`, а PRD-срезы синхронизированы с новым правилом CTA для этого блока.

## 2026-03-20
- `Docs/AGENTS.md` синхронизирован с новым master-документом `Docs/WebCode_landind_PRD.md`.
- `Docs/webcode_content_only.md` выведен из использования и удалён из проекта как устаревший источник контента.
- Добавлена папка `Docs/PRD/` с навигационными extracts по PRD для быстрой работы без загрузки всего документа в контекст.
- В `AGENTS.md` обновлены источники истины, структура лендинга и правило приоритета документов.
- В проект инициализирован `shadcn/ui`: добавлены `components.json`, `lib/utils.ts`, базовый `components/ui/button.tsx`, обновлены зависимости и `app/globals.css` под shadcn theme/tailwind setup.
- Секция `Для кого подходит WebCode` синхронизирована с PRD: возвращены 4 канонические карточки, исправлены локализации `RU / UZ / EN`, убраны лишние CTA и продуктовые отклонения от master PRD.
- Для секции `Для кого подходит WebCode` добавлены hover-состояния карточек и scroll-linked reveal с последовательным stagger, привязанным к реальной прокрутке секции.
- В `Docs/design_direction.md` зафиксированы рабочие правила `scroll reveal` для секций ниже HERO: привязка к реальной прокрутке, stagger intro/cards, межрядная задержка и финал анимации к низу секции.

## 2026-03-19
- Создан базовый пакет документации процесса для проекта WebCode.
- Добавлены документы: `Roadmap.md`, `log.md`, `design_direction.md`, `offers_and_positioning.md`, `done_criteria.md`.
- Обновлены правила в `AGENTS.md` под практичный workflow разработки лендинга.
- Зафиксированы требования по i18n, компонентам, SEO, контентным заглушкам и проверкам качества.
- Инициализирован базовый Next.js 16 scaffold с TypeScript, Tailwind CSS v4 и ESLint.
- Подключён `next-intl`, настроены локализованные маршруты `RU / UZ / EN`.
- Добавлена стартовая структура `components`, `i18n`, `messages`, `lib`.
- Шаблон `create-next-app` заменён на стартовую локализованную страницу WebCode.
- Удалены все временные блоки лендинга, проект приведён к чистому scaffold-состоянию.
- Локализованная страница упрощена до пустого layout-контейнера под будущую сборку интерфейса.
- Собрана новая HERO секция по предоставленному image-reference: тёмная премиальная палитра, `Manrope + Inter`, пустая правая visual-зона под будущий asset.
- Layout HERO переработан: декоративные элементы привязаны к каркасу секции, правая зона оформлена как явный placeholder, уменьшен визуальный хаос и улучшена структура desktop-композиции.
- `Docs/design_direction.md` переписан в формат рабочего дизайн-кода проекта на основе утверждённой HERO секции.
- HERO секция доведена до финального состояния: desktop/mobile layout, motion, locale switcher, переводы `RU / UZ / EN`, orbit badge и stats block зафиксированы как эталон для следующих секций.
- `Docs/elements.md` обновлён под фактическую структуру и спецификацию утверждённой HERO секции.
- В HERO обновлён логотип с versioned local image source и настроен `images.localPatterns` для локальных изображений.
- Уточнены тексты и локализация HERO: бренд `WebCode` в описании подсвечен фирменным цветом, mobile badge сокращён отдельно для `RU / UZ / EN`.
- Исправлена локализация мобильного меню и предупреждение Next.js для `scroll-behavior` через `data-scroll-behavior="smooth"` на корневом `<html>`.
- Исправлен fullscreen overlay мобильного меню: вынесен из анимированного `header`, восстановлен фон, добавлены блокировка scroll и закрытие по `Escape`.
- Добавлена секция `Для кого подходит` как отдельный компонент с переводами `RU / UZ / EN`, якорем `#audience` и карточной сеткой категорий бизнеса.
- Секция `Для кого подходит` пересобрана под обновлённый `webcode_content_only.md`: убраны лишние вспомогательные блоки, обновлены заголовок, описание и 6 категорий бизнеса.
- Header вынесен из `HeroSection` в отдельный компонент `SiteHeader`, подключён на уровне страницы и синхронизирован с якорной навигацией.
- Для `SiteHeader` добавлено состояние прозрачного фона по умолчанию и проявление фирменного фона только после начала прокрутки.
- Документация проекта синхронизирована с фактической структурой первого экрана: `SiteHeader` и `AudienceSection` зафиксированы как актуальные implementation units.
