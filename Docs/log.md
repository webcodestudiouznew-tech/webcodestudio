# log.md

## 2026-03-24
- Все CTA, которые раньше вели на секцию `#lead`, переведены на единый modal-flow: теперь вместо скролла открывается popup с текущей формой заявки, а основной фон страницы при открытии заметно блюрится и затемняется.
- Добавлены client-side `LeadFormModalProvider` и `LeadModalTrigger`, чтобы modal открывался из `Hero`, `Pricing`, `Cases`, `Process`, `Why WebCode`, `Solution Includes`, `Business Benefits` и из header без дублирования логики формы.
- Для popup добавлена явная кнопка `Закрыть / Yopish / Close`, а локализации формы расширены новым заголовком модального окна; `npm run lint` и `npm run build` после изменений прошли успешно.
- Выполнен keyword pass по `Docs/Tasks/SEO_keywords.md`: обновлены `Metadata.title` и `Metadata.description` для `RU / UZ / EN` под приоритетные commercial queries `создание сайтов в Ташкенте / Toshkentda sayt yaratish / website development in Tashkent`.
- Усилен SEO intent в hero, pricing, FAQ, lead section и footer microcopy: добавлены естественные вхождения `landing page`, `корпоративный сайт`, `сайт под ключ`, `multilingual site`, `CRM`, `Telegram / WhatsApp`, `domain` и `hosting` без изменения архитектуры страниц.
- `FAQ` расширен новыми long-tail вопросами по стоимости сайта, срокам разработки, мультиязычности, составу `turnkey` запуска и подключениям `CRM + Telegram` во всех локалях.
- Повторно прогнаны `npm run lint` и `npm run build`; обе проверки завершились успешно.
- Закрыт pre-deploy SEO checklist из `Docs/Tasks/SEO.md`: `/` переведён на `permanentRedirect("/ru")`, добавлена indexable страница `Privacy Policy` для `RU / UZ / EN`, `footer` теперь ведёт на реальную локализованную страницу, а год в нижней строке стал динамическим.
- `sitemap.xml` расширен и теперь включает локализованные homepage и privacy routes с корректными `alternate language URLs`; `canonical`/`alternates` для privacy page также добавлены через общую SEO-логику.
- `Open Graph` image сделан locale-aware через маршрут `app/(site)/[locale]/opengraph-image.tsx`, а WhatsApp prefilled message переведён в locale-aware helper для `ru / uz / en`.
- Повторно подтверждено, что на homepage сохраняется один SEO-safe `H1` в `hero-section`; проверки `npm run lint` и `npm run build` завершились успешно.
- В guide-документах зафиксировано правило для будущих мультиязычных лендингов: корректный `html lang` обязателен для каждой локали на уровне server-rendered HTML, а локализованные App Router-страницы нужно изначально проектировать через отдельные root layouts или route groups, а не через request headers.
- Закрыт основной SEO-слой лендинга: добавлены локализованные `canonical` и `alternate` links, а также `Open Graph` и `Twitter` metadata для `RU / UZ / EN`.
- Добавлены `robots.ts` и `sitemap.ts` для индексации, а также `schema.org` JSON-LD на локализованную главную страницу.
- Добавлен route `opengraph-image` для share-preview и явное подключение `favicon` в metadata корневого layout.
- Добавлена новая секция `Отзывы` как отдельный компонент с подключением в страницу между `Кейсы` и `Тарифы`.
- Для секции `Отзывы` реализован формат из двух медленных бегущих строк с карточками в тёплом светлом visual language, синхронизированном с design DNA проекта.
- В карточки отзывов добавлены абстрактные аватары с инициалами вместо фото и локализованный контент `RU / UZ / EN`.
- Прогнана проверка `npm run lint`, ошибок не обнаружено.

## 2026-03-23
- В локальном окружении подключены реальные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_GROUP_CHAT_ID`; отправка тестового сообщения в группу `WebCode leads` успешно подтверждена.
- Реализован серверный слой уведомлений о новых заявках в `Telegram` через `Telegram Bot API`: после сохранения формы сервер пытается отправить сообщение в рабочую группу, но не ломает success-state формы при временной недоступности `Telegram`.
- Добавлен guide `Guides/telegram_bot_group_setup.md` с инструкцией по созданию бота, подключению группы, получению `TELEGRAM_GROUP_CHAT_ID` и настройке env; `Docs/AGENTS.md` обновлён ссылкой на этот документ.
- Добавлена новая рабочая задача `Docs/Tasks/telegram_bot_group_lead_alerts.md` на реализацию уведомлений о новых заявках в `Telegram` через связку `bot + group`; задача также отражена в `Roadmap.md` как ближайший следующий шаг.
- Добавлен guide `Guides/notion_leads_integration.md` с описанием процесса подключения формы заявок к `Notion`, схемы базы, env-переменных и проверки записи лидов; `Docs/AGENTS.md` обновлён ссылкой на этот документ.
- Подключена живая интеграция формы заявки с `Notion` через `NOTION_TOKEN` и `NOTION_DATABASE_ID` в локальном окружении проекта; сохранение лидов больше не работает в `pending_notion` режиме при наличии этих env-переменных.
- Документация проекта расширена до системы guide-документов: добавлены `Guides/landing_ui_standards.md`, `Guides/mobile_and_responsive.md`, `Guides/localization_standards.md`, `Guides/conversion_standards.md`, `Guides/accessibility_and_quality.md`.
- `Docs/AGENTS.md` обновлён и теперь работает как короткая master-инструкция со ссылками на профильные стандарты для UI, responsive, localization, conversion и quality.
- Добавлена новая секция `LeadSection` с якорем `#lead`, которая закрывает этап `Формы и конверсия`: слева быстрые каналы связи, справа форма заявки.
- Реализована форма с полями `Имя`, `Телефон`, `Telegram nickname`, `Ниша бизнеса`; обязательными остались `Имя` и `Телефон`.
- Для формы подключены `react-hook-form + zod`, success-state после отправки и локализованные тексты `RU / UZ / EN`.
- Добавлен server action `submitLeadForm` с подготовленной интеграцией под `Notion`: при отсутствии `NOTION_TOKEN` и `NOTION_DATABASE_ID` запись пока уходит в безопасный pending-режим без падения UI.
- Добавлены рабочие CTA для `Telegram` и `WhatsApp`, привязанные к реальным ссылкам пользователя (`@uz_sellium`, `+998901783138`) в `Hero`, `FAQ`, новой lead-секции и `Footer`.
- Добавлен базовый клиентский слой аналитики с событиями `form_view`, `form_submit`, `form_success`, `telegram_click`, `whatsapp_click` через `dataLayer` и custom event.
- Прогнаны проверки `npm run lint` и `npm run build`, обе завершились успешно.

## 2026-03-22
- Добавлена секция `FAQ` как отдельный компонент с одноколоночной accordion-подачей, 9 каноническими вопросами из PRD и повторным CTA-блоком после списка.
- Для секции `FAQ` добавлены локализации `RU / UZ / EN`, а также выполнен проход по interaction states: плотность списка, hover-состояния и корректировка мобильной подачи.
- Добавлен `Footer` как отдельный компонент с якорем `#contacts`, брендом `WebCode`, минималистичным меню секций и нижней служебной строкой.
- Для `Footer` добавлены локализации `RU / UZ / EN`, выполнен проход по desktop/mobile-раскладке, выравниванию меню, логотипу и визуальной плотности.
- Секция `Финальный CTA` по итоговому решению пользователя удалена из страницы и связанной локализации, чтобы основной контур лендинга завершался на `Footer`.
- Добавлена секция `Тарифы` как отдельный компонент с якорем `#pricing`, тремя пакетами (`Запуск`, `Доверие`, `Рост`) и синхронизированными локализациями `RU / UZ / EN`.
- Секция `Тарифы` прошла несколько итераций по visual language и mobile-адаптации: уплотнены карточки, сокращён заголовок, переработан блок `Во всех тарифах включено`, а состав `Что входит` обновлён под согласованные формулировки пользователя.
- Добавлена секция `Как проходит работа` как отдельный компонент с якорем `#process`, четырьмя каноническими шагами из PRD и отдельным CTA-блоком в стилистике секции `Почему WebCode`.
- Для секции `Как проходит работа` добавлены локализации `RU / UZ / EN`, а промежуточный информационный блок про готовое ТЗ удалён по уточнению пользователя.
- Добавлена секция `Что получает бизнес` как отдельный компонент с ровной сеткой `3x2`, CTA-блоком снизу и якорем `#business-benefits`.
- Для секции `Что получает бизнес` добавлены локализации `RU / UZ / EN`, синхронизированные с каноническим PRD без новых продуктовых смыслов вне документа.
- Мобильная версия CTA в секции `Что получает бизнес` отцентрована, а заголовок CTA сокращён и зафиксирован в одну строку во всех трёх локалях.
- Секция подключена в [locale]-страницу после `Почему WebCode`.
- Добавлена секция `Что входит в решение` как отдельный компонент с якорем `#solution-includes`, двухколоночным основным блоком, отдельным блоком `Что не входит` и мягким CTA.
- Для секции `Что входит в решение` добавлены и уточнены локализации `RU / UZ / EN`, включая формулировку о том, что стоимость домена и хостинга включена в тариф.
- В секции `Что входит в решение` выполнен проход по mobile-адаптации и выравниванию: центрирование заголовка и описания блока `Что не входит`, выравнивание мини-карточек и корректировка плотности CTA.
- Добавлена секция `Кейсы` с двумя реальными проектами (`Sellium`, `PEAKS`), подключёнными в страницу после `Что входит в решение`.
- Секция `Кейсы` пересобрана в более компактный формат: реальные скриншоты из `public`, accordion-подача деталей, встроенный CTA в каждом кейсе и без отдельного общего CTA-блока снизу.
- Для секции `Кейсы` добавлены и уточнены локализации `RU / UZ / EN`, включая списки `Что сделали`, `Что получил бизнес`, корректные названия кнопок и сокращённый заголовок секции.
- В секции `Кейсы` выполнен проход по mobile-адаптации и визуальной плотности: переработаны пропорции блока со скриншотом, выравнивание правой колонки, горизонтальное растяжение CTA-кнопок и сглаженная анимация аккордеонов.
- Прогнаны проверки `npm run lint` и `npm run build`, обе завершились успешно.

## 2026-03-21
- Секция `Почему WebCode` доведена до рабочего состояния: карточки переработаны в bento-подобную desktop-сетку, синхронизированы тексты `RU / UZ / EN`, добавлен и стилистически доработан CTA-блок.
- Проведён большой проход по mobile-адаптации `Hero`, `Audience` и `Why WebCode`: исправлены заголовки, spacing, card rhythm, CTA-композиция и поведение на узких экранах около `360px`.
- В `Hero` обновлён основной оффер на `Готовый сайт для бизнеса с CRM системой` и синхронизированы переводы для `RU / UZ / EN`.
- Прогнаны финальные проверки `npm run lint` и `npm run build`, обе завершились успешно.
- Добавлена секция `Почему WebCode` как отдельный компонент с якорем `#why-webcode`, сеткой `2x3` и контентом, синхронизированным с каноническим PRD.
- Для секции `Почему WebCode` добавлены локализации `RU / UZ / EN` и синхронизирован визуальный язык карточек с остальными секциями лендинга.
- Из секций лендинга удалена scroll-reveal анимация: убраны view-timeline стили и привязанные reveal-классы в реализации блоков.
- В секцию `Почему WebCode` добавлен обязательный inline CTA `Обсудить ваш проект`, а PRD-срезы синхронизированы с новым правилом CTA для этого блока.

## 2026-03-20
- `Docs/AGENTS.md` синхронизирован с новым master-документом `Docs/WebCode_landind_PRD.md`.
- `Docs/webcode_content_only.md` выведен из использования и удалён из проекта как устаревший источник контента.
- Добавлена папка `Docs/PRD/` с навигационными extracts по PRD для быстрой работы без загрузки всего документа в контекст.
- В `AGENTS.md` обновлены источники истины, структура лендинга и правило приоритета документов.
- В проект инициализирован `shadcn/ui`: добавлены `components.json`, `lib/utils.ts`, базовый `components/ui/button.tsx`, обновлены зависимости и `app/globals.css` под shadcn theme/tailwind setup.
- Секция `Для кого подходит WebCode` синхронизирована с PRD: возвращены 4 канонические карточки, исправлены локализации `RU / UZ / EN`, убраны лишние CTA и продуктовые отклонения от master PRD.
- Для секции `Для кого подходит WebCode` добавлены hover-состояния карточек и scroll-linked reveal с последовательным stagger, привязанным к реальной прокрутке секции.
- В `Docs/design_direction.md` зафиксированы рабочие правила `scroll reveal` для секций ниже HERO: привязка к реальной прокрутке, stagger intro/cards, межрядная задержка и финал анимации к низу секции.

## 2026-03-19
- Создан базовый пакет документации процесса для проекта WebCode.
- Добавлены документы: `Roadmap.md`, `log.md`, `design_direction.md`, `offers_and_positioning.md`, `done_criteria.md`.
- Обновлены правила в `AGENTS.md` под практичный workflow разработки лендинга.
- Зафиксированы требования по i18n, компонентам, SEO, контентным заглушкам и проверкам качества.
- Инициализирован базовый Next.js 16 scaffold с TypeScript, Tailwind CSS v4 и ESLint.
- Подключён `next-intl`, настроены локализованные маршруты `RU / UZ / EN`.
- Добавлена стартовая структура `components`, `i18n`, `messages`, `lib`.
- Шаблон `create-next-app` заменён на стартовую локализованную страницу WebCode.
- Удалены все временные блоки лендинга, проект приведён к чистому scaffold-состоянию.
- Локализованная страница упрощена до пустого layout-контейнера под будущую сборку интерфейса.
- Собрана новая HERO секция по предоставленному image-reference: тёмная премиальная палитра, `Manrope + Inter`, пустая правая visual-зона под будущий asset.
- Layout HERO переработан: декоративные элементы привязаны к каркасу секции, правая зона оформлена как явный placeholder, уменьшен визуальный хаос и улучшена структура desktop-композиции.
- `Docs/design_direction.md` переписан в формат рабочего дизайн-кода проекта на основе утверждённой HERO секции.
- HERO секция доведена до финального состояния: desktop/mobile layout, motion, locale switcher, переводы `RU / UZ / EN`, orbit badge и stats block зафиксированы как эталон для следующих секций.
- `Docs/elements.md` обновлён под фактическую структуру и спецификацию утверждённой HERO секции.
- В HERO обновлён логотип с versioned local image source и настроен `images.localPatterns` для локальных изображений.
- Уточнены тексты и локализация HERO: бренд `WebCode` в описании подсвечен фирменным цветом, mobile badge сокращён отдельно для `RU / UZ / EN`.
- Исправлена локализация мобильного меню и предупреждение Next.js для `scroll-behavior` через `data-scroll-behavior="smooth"` на корневом `<html>`.
- Исправлен fullscreen overlay мобильного меню: вынесен из анимированного `header`, восстановлен фон, добавлены блокировка scroll и закрытие по `Escape`.
- Добавлена секция `Для кого подходит` как отдельный компонент с переводами `RU / UZ / EN`, якорем `#audience` и карточной сеткой категорий бизнеса.
- Секция `Для кого подходит` пересобрана под обновлённый `webcode_content_only.md`: убраны лишние вспомогательные блоки, обновлены заголовок, описание и 6 категорий бизнеса.
- Header вынесен из `HeroSection` в отдельный компонент `SiteHeader`, подключён на уровне страницы и синхронизирован с якорной навигацией.
- Для `SiteHeader` добавлено состояние прозрачного фона по умолчанию и проявление фирменного фона только после начала прокрутки.
- Документация проекта синхронизирована с фактической структурой первого экрана: `SiteHeader` и `AudienceSection` зафиксированы как актуальные implementation units.
