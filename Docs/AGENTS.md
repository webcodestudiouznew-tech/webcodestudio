# AGENTS.md — Контекст проекта WebCode и правила для агентов.

Этот файл содержит ключевую информацию для всех AI-агентов, работающих над проектом. Используйте его для понимания целей, стека и правил.

## 1. Описание Проекта
**WebCode** — это лендинг веб-студии, специализирующейся на создании сайтов для локального офлайн-бизнеса в Узбекистане (салоны красоты, клиники, автосервисы).
**Ключевое предложение:** Сайт + CRM (Notion) + Интеграция мессенджеров + Домен/Хостинг за один платеж.

## 2. Технологический Стек
Подробности в tech_stack.md
- **Core:** Next.js 16 (App Router) + TypeScript
- **Styling:** Tailwind CSS v4 + shadcn/ui (New York style)
- **i18n:** next-intl v4.4 (Поддержка RU, UZ, EN)
- **Forms:** react-hook-form + zod
- **Package Manager:** npm

## 3. Источники истины
- [tech_stack.md](./tech_stack.md) — утверждённый стек.
- [webcode_content_only.md](./webcode_content_only.md) — тексты по секциям.
- [offers_and_positioning.md](./offers_and_positioning.md) — позиционирование, оффер, приоритет CTA.
- [design_direction.md](./design_direction.md) — визуальное направление.
- [Roadmap.md](./Roadmap.md) — статус и этапы проекта.
- [log.md](./log.md) — история значимых изменений.
- [done_criteria.md](./done_criteria.md) — критерии готовности.

## 4. Структура Лендинга
Секции:
1. **Header:** 
2. **Hero:** 
3. **Для кого:** 
4. **Какие проблемы решает WebCode** 
5. **Что входит в сайт под ключ** 
6. **Тарифы:** 
7. **Как проходит работа:** 
8. **Отзывы**
9. **FAQ**
10. **Финальный блок** 
11. **футер**

## 5. 🚨 КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ АГЕНТОВ

### ⚠️ ВСЕГДА ЗАПРАШИВАТЬ РАЗРЕШЕНИЕ ПЕРЕД:

- ✋ Изменением конфигурационных файлов (`package.json`, `tsconfig.json`, `next.config.*`, `eslint.config.*`)
- ✋ Установкой новых зависимостей или крупных наборов компонентов
- ✋ Удалением файлов или папок
- ✋ Крупными архитектурными изменениями, которые меняют структуру проекта

### ✅ МОЖНО ДЕЛАТЬ БЕЗ ДОПОЛНИТЕЛЬНОГО СОГЛАСОВАНИЯ В РАМКАХ ЗАДАЧИ:

- Создавать обычные проектные файлы компонентов, секций, утилит и документов
- Редактировать существующие файлы реализации
- Добавлять локализации, контентные файлы и вспомогательные UI-компоненты
- Улучшать структуру кода, если это не требует смены архитектуры или зависимостей

### 🚫 КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:

- ❌ Начинать работу без изучения документации
- ❌ Нарушать стандарты качества (0 ошибок TypeScript/ESLint)
- ❌ Настраивать компоненты в местах использования
- ❌ Устанавливать UI компоненты без проверки через shadcn MCP
- ❌ Забывать обновлять документацию после задач
- ❌ Хардкодить пользовательские тексты прямо в UI-компонентах
- ❌ Использовать фейковые отзывы, кейсы или неподтверждённые метрики
- ❌ Ломать мультиязычность ради локального удобства в одной секции
- ❌ Самостоятельно добавлять новый контент, поясняющие тексты, смысловые блоки или продуктовые формулировки, если пользователь прямо не просил об этом
- ❌ Считать задачу завершённой или закрывать её без явного подтверждения пользователя, что задача выполнена

### 🌐 Языковые правила:

- **Всегда отвечай на русском языке**
- **НЕ переводи:** технические термины, команды, пути к файлам
- **Переводи:** объяснения, комментарии, UI текст

## 6. Правила проектирования и реализации

### i18n
- Проектировать все секции сразу под `RU / UZ / EN`.
- Не размещать UI-тексты напрямую в JSX, если для них предполагается локализация.
- Учитывать, что длина строк в разных языках отличается.
- Не делать layout, который держится только на одной длине заголовков.

### Компоненты
- Каждая крупная секция лендинга должна быть отдельным компонентом.
- Повторяемые паттерны (`Section`, `CTA`, карточки, badge, section heading) выносить в переиспользуемые компоненты.
- Не смешивать в одном компоненте layout, контент и интеграционную логику без необходимости.
- Контент должен быть максимально отделён от представления.

### Контентные заглушки
- Если реальных кейсов, отзывов, цифр или логотипов клиентов нет, использовать только честные временные заглушки.
- Все временные заглушки должны быть явно распознаваемы как временные.
- Нельзя придумывать достижения, клиентов или результаты ради заполнения секции.

### SEO и аналитика
- Каждый экран и локаль должны иметь корректные metadata.
- Нужно готовить `title`, `description`, Open Graph и базовую semantic structure.
- CTA должны быть готовы к аналитической разметке событий.
- Локализованные страницы должны учитывать `canonical` и `alternate` связи.

## 7. Рекомендуемый workflow

Порядок работы по умолчанию:

1. Изучить `AGENTS.md` и связанные документы в `Docs`.
2. Проверить `Roadmap.md`, чтобы понять текущий этап.
3. Выполнить задачу в логичном порядке:
   - Discovery
   - Structure
   - Visual direction
   - App scaffold
   - Sections
   - Forms / CRM hooks
   - i18n
   - SEO
   - QA
4. После изменений обновить `Roadmap.md` при необходимости.
5. Зафиксировать важные изменения в `log.md`.

## 8. 🛠️ Обязательные инструменты:

1. **Context7 MCP** - для актуальной документации библиотек
2. **shadcn MCP** - для поиска UI компонентов
3. **stitch MCP** - для переноса дизайна страниц в проект из google stitch

## 9. Минимальный чеклист для проверки:

npm run lint — прогнать ESLint.
npm run build — убедиться, что проект собирается (поймает ошибки типов/конфигурации).
npm run dev — локально посмотреть, что страница рендерится и шрифты/metadata работают (ручная проверка в браузере).

Дополнительно обязательно:
- Проверить mobile viewport.
- Проверить все основные CTA.
- Проверить работу формы заявки.
- Проверить RU / UZ / EN.
- Проверить logo, favicon и metadata.
